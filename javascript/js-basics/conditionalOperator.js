// ## 三項演算子

// 三項演算子は、条件式を評価して、その結果によって値を返す演算子です。
// 三項演算子は、次のように記述します。
// 条件式 ? trueの場合の値 : falseの場合の値
// 例)
const age = 20;
const message = age >= 20 ? "Adult" : "Child";
console.log(message); // => Adult
